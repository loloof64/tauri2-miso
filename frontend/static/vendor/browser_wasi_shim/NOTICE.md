Vendored from [`@bjorn3/browser_wasi_shim`](https://github.com/bjorn3/browser_wasi_shim) v0.3.0
(`dist/` output only), so the app can run fully offline instead of loading it from a CDN.

Dual-licensed MIT OR Apache-2.0 — see `LICENSE-MIT` / `LICENSE-APACHE` in this directory.

To update: `npm pack @bjorn3/browser_wasi_shim@<version>`, extract, and replace the `.js`
files here with the new `dist/*.js` (keep this directory's licenses in sync too).
