{
  "name": "tauri2-miso",
  "private": true,
  "version": "0.0.5",
  "type": "module",
  "scripts": {
    "dev": "cd frontend && make build && make serve",
    "build": "cd frontend && make build",
    "tauri": "tauri",
    "android:dev": "tauri android dev --host 127.0.0.1",
    "prepare": "husky"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@tauri-apps/cli": "^2",
    "cm-chessboard": "^8.12.19",
    "husky": "^9.1.7"
  }
}
